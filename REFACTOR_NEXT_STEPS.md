# Refactor Next Steps

다음 작업 때 이어서 할 일:

1. 현재 로컬 리팩터링 변경분을 한 번 더 훑어본다.
2. 필요하면 `python3 update_movies.py`를 실제 실행해서 데이터 출력 개수를 확인한다.
3. 이상 없으면 리팩터링 변경분을 커밋한다.
4. 이후 선택 작업으로 `update_movies.py`의 TMDB/KOBIS 조회 로직을 별도 모듈로 분리한다.
